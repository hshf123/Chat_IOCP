syntax = "proto3";
package Protocol;

import "Enum.proto";

message Player
{
	uint64 id = 1;
	string name = 2;
	uint32 roomId = 3;
}

message RoomInfo
{
	uint32 id = 1;
	uint32 playerCount = 2;
	repeated Player players = 3;
}